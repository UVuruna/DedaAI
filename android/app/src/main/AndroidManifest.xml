<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- In-app self-update: hand the downloaded release APK to Android's
         package installer (Obtainium/F-Droid pattern). The user still
         confirms every install in the system dialog. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Voice commands (Settings toggle, off by default; granted at runtime
         only when the user switches them on). Deliberately NOT here:
         RECEIVE_SMS / READ_SMS — on Google's Play Protect sideload
         hard-block list, the same bucket that killed the notification
         listener (2026-08-21 research). -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:name=".DedaApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="DedaAI"
        android:supportsRtl="true"
        android:theme="@style/Theme.CameraAccess">

        <!-- Without Developer Mode, this key will need to be set with ID from app registered in Wearables Developer Center -->
        <meta-data
            android:name="com.meta.wearable.mwdat.APPLICATION_ID"
            android:value="0"
            />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CameraAccess">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="cameraaccess" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- Foreground service for background streaming -->
        <service
            android:name=".stream.StreamingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Deda: the glasses' touchpad buttons (double tap = standby) and,
             since Phase 3, the wake-word standby listening (microphone type so
             the mic keeps working with the screen locked) -->
        <service
            android:name=".deda.GlassesButtonService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|microphone" />

        <!-- NO notification listener here, on purpose (2026-08-20): declaring
             one made Google Play Protect hard-block the sideloaded install
             ("sensitive data", no Install-anyway button). It only served
             handing single taps back to the music app; music resume now rides
             the transient audio-focus contract instead. -->

        <!-- Installing a self-update kills the process — this manifest-declared
             receiver is the only way to hear that it finished and invite the
             user back in (activity start when allowed, notification always). -->
        <receiver
            android:name=".update.UpdateReturnReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
